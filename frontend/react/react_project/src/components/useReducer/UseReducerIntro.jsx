import React from 'react'
import UseReducerTask1 from './tasks/UseReducerTask1'
import UseReducerTask2 from './tasks/UseReducerTask2'

const UseReducerIntro = () => {
  return (
    <div>
        {/* <UseReducerTask1></UseReducerTask1> */}
        <UseReducerTask2></UseReducerTask2>
    </div>
  )
}

export default UseReducerIntro




// Counter App

// Create a counter with + and - buttons.

// Use useReducer instead of useState to manage the count.

// Counter with Reset

// Extend the counter app.

// Add a reset button that sets the count back to 0.

// Toggle Light On/Off

// Create a button that toggles between "Light ON" and "Light OFF" states using useReducer.

// Simple Form Input

// Manage a form with two fields (name and email) using useReducer.

// Display the entered data below the form.

// Todo List (Add Items)

// Build a simple todo list.

// Use useReducer to handle adding new todos.

// Todo List (Add + Remove Items)

// Extend the todo list.

// Allow removing todos by clicking a delete button.

// Shopping Cart (Add to Cart)

// Have a list of products with "Add to Cart" buttons.

// Use useReducer to maintain cart items.

// Shopping Cart (Add + Remove Items)

// Extend the cart.

// Add a remove button to remove items from the cart.

// Accordion (Open/Close Sections)

// Build a simple accordion where clicking on a section toggles it open/closed.

// Manage the open/closed state with useReducer.

// Theme Switcher

// Use useReducer to toggle between light theme and dark theme.

// Apply different background/text colors based on the current theme.