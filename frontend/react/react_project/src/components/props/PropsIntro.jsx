import PropsP1 from "./Tasks/PropsP1";

let PropsIntro = () => {
  return (
    <div>
      <PropsP1></PropsP1>
    </div>
  );
};
export default PropsIntro;



// Pass Your Name

// Create a Greeting component that accepts a name prop.

// It should display: Hello, [name]!

// Props for Colors

// Create a ColoredBox component that accepts a color prop.

// Render a <div> with the background color set to the prop value.

// Props for Button Text

// Make a CustomButton component that accepts a text prop.

// Use it to show different buttons like “Submit”, “Cancel”, etc.

// Props for Images

// Create an Avatar component that accepts src and alt props.

// Display an image using these props.

// Props with Multiple Values

// Build a UserCard component that accepts name, age, and city props.

// Show them in a nice card layout.

// Props with Numbers

// Make a CounterDisplay component that accepts a count prop.

// Display the text: Current Count: [count].

// Props for Lists

// Create a List component that accepts an array of strings via a prop.

// Render them as <li> items inside a <ul>.

// Boolean Props
// Build a Status component that accepts a boolean isOnline prop.
// Show “🟢 Online” if true and “🔴 Offline” if false.

// Props with Default Values

// Make a Profile component that accepts name and role props.

// If role is not provided, set the default to "Student".

// Props for Children

// Create a Card component that uses props.children.

// Wrap some text inside <Card> ... </Card> and display it.